{
  "name": "range-pool",
  "version": "3.1.0",
  "description": "A base library for creating range-based worker pools",
  "main": "lib/index.js",
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-steelbrain": "^5.0.0",
    "eslint-config-steelbrain": "^3.0.0",
    "flow-bin": "^0.55.0"
  },
  "scripts": {
    "clean": "rm -rf lib",
    "build": "npm run clean; babel src --out-dir lib",
    "watch": "npm run clean; babel src --out-dir lib --watch",
    "test": "(apm test) && (flow check) && (eslint . )"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/steelbrain/range-pool.git"
  },
  "keywords": [
    "range",
    "pool",
    "worker"
  ],
  "author": "steelbrain",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/steelbrain/range-pool/issues"
  },
  "homepage": "https://github.com/steelbrain/range-pool#readme"
}
